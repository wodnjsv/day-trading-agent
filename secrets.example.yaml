# 비밀 설정 템플릿 — secrets.yaml로 복사 후 실제 값 입력 (secrets.yaml은 git 무시됨)

# --- KIS (한국투자증권) — manju 수집기/주문 ---
app_key: "발급받은_appkey"
app_secret: "발급받은_appsecret"
account_no: "12345678"
is_paper: false
data_dir: "data"
universe_size: 20

# --- KRX OpenAPI (data-dbg.krx.co.kr) — jongga 백테스트 가격·시총·PIT ---
krx_api_key: "발급받은_KRX_OpenAPI_인증키"

# --- OpenAI (GPT-5.4) — jongga 재료조사/Selector (Phase 2) ---
openai_api_key: "발급받은_OpenAI_API_키"
